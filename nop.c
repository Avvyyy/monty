#include <stdio.h>
#include "monty.h"

/**
 * nop- Function that does nothing
 * @stack:Pointer to top of stack
 * @line_number: Line number for nop function
 */

void nop(stack_t **stack, unsigned int line_number)
{
	 (void)stack;
	(void)line_number;
}
